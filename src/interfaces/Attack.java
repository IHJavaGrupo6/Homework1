package interfaces;

public interface Attack {

    //int id, string name, int hp, boolean isAlive

    int getId (int id);
    String getName(String name);
    int getHp(int hp);
    boolean isAlive(boolean isAlive);
}
